extern crate inapp;

// use builder::app;

fn main() {
    // let args = builder::parse_args();
    // println!("{}", args.platform);
    // let app = builder::app::App::new();
    // app.parse_config_str();
    // let out = app.build_csv();
    // println!("Out csv: \n{}\n", out);
}